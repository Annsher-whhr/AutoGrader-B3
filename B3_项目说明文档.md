# B3 项目说明文档

## 1. 项目概述

本项目实现的是 `AutoGrader` 系统中的 `B3` 模块，当前只覆盖 `problem.txt` 中 **第 2 题到第 10 题** 的判题需求，第 1 题邮件标题格式不纳入评测。

`B3` 当前定位是一个独立后端服务，负责：

- 题库管理
- 测试数据维护
- 代码/命令安全控制
- 评测执行
- 评测结果输出

服务采用：

- `FastAPI`
- `SQLAlchemy`
- `MySQL`
- `PyMySQL`

API 前缀为：

```text
/api/v1/b3
```

默认端口：

```text
8003
```

---

## 2. 我完成了什么

### 2.1 已实现的核心能力

当前版本已经完成一个可运行的 B3 演示版，实现了题库、判题、安全检查和评测结果输出的最小闭环。

#### 1. 代码安全控制

已实现基础安全检查，主要用于限制危险 shell 命令和危险脚本写法：

- 命令白名单控制
- 危险命令拦截
  - 如 `rm`、`sudo`、`shutdown`、`curl`、`wget` 等
- 危险 shell 语法拦截
  - 如 `&&`、`||`、重定向、反引号、路径穿越等
- 脚本死循环特征检测
  - 如 `while true`
- 非法路径访问检测
  - 如 `../`

对应实现文件：

- [security_checks.py](D:\vscode python\b3\app\judge\security_checks.py)

#### 2. 三种测试框架

虽然本次重点只评 9 道题，但内部已经保留三类框架：

- 命令型题目
  - 如 `ssh`、`who`、`uname`、`date`、`cal`、`cat`
- 文件型题目
  - 如 `head`、`tail`、`cp`、`mv`、`sed`、`awk`
- API 调用型题目
  - 额外实现了一个 `API_DEMO` 演示题
  - 支持导入 Python 代码并调用指定函数

对应实现文件：

- [shell_checks.py](D:\vscode python\b3\app\judge\shell_checks.py)
- [api_runner.py](D:\vscode python\b3\app\judge\api_runner.py)
- [evaluation_service.py](D:\vscode python\b3\app\services\evaluation_service.py)

#### 3. 题库管理

已实现题库和测试数据的存储与管理，支持：

- 预置导入题目
- 查询题目列表
- 查询题目详情
- 查询题目测试用例
- 更新题目信息

当前已经预置：

- `Q02` 到 `Q10`
- `API_DEMO`

对应实现文件：

- [seed_data.py](D:\vscode python\b3\app\seed_data.py)
- [question_service.py](D:\vscode python\b3\app\services\question_service.py)
- [models.py](D:\vscode python\b3\app\models.py)

#### 4. 评测结果生成

评测结果统一返回：

- 总评分 `overall_score`
- 通过用例数 `passed_count`
- 总用例数 `total_count`
- 总体评价 `overall_comment`
- 安全问题 `static_issues`
- 细节评分 `case_results`

每个 `case_result` 包含：

- `case_id`
- `description`
- `passed`
- `score`
- `actual_output`
- `expected_output`
- `error`
- `execution_time_ms`

---

## 3. 当前支持的题目

当前题库中已经实现的题目如下：

| 题号 | 题目ID | 内容 |
| --- | --- | --- |
| 第2题 | `Q02` | SSH 登录命令 |
| 第3题 | `Q03` | who / uname / date / cal / cat |
| 第4题 | `Q04` | cd / pwd / cat |
| 第5题 | `Q05` | head / tail / ls / cp / mv |
| 第6题 | `Q06` | vi 参数命令 + chmod |
| 第7题 | `Q07` | grep / sort |
| 第8题 | `Q08` | sed |
| 第9题 | `Q09` | awk |
| 第10题 | `Q10` | shell 脚本输出非素数 |
| 演示题 | `API_DEMO` | API 调用型 add(a, b) |

说明：

- `Q02-Q10` 是本次真正业务范围
- `API_DEMO` 是为了满足“API 调用型测试框架必须实现”的要求

---

## 4. 项目结构

```text
b3/
├─ app/
│  ├─ core/
│  │  └─ config.py              配置
│  ├─ judge/
│  │  ├─ api_runner.py          API 调用型执行器
│  │  ├─ security_checks.py     安全检查
│  │  └─ shell_checks.py        9 道题的判题规则
│  ├─ services/
│  │  ├─ evaluation_service.py  评测服务
│  │  └─ question_service.py    题库服务
│  ├─ db.py                     数据库连接
│  ├─ main.py                   FastAPI 入口
│  ├─ models.py                 数据库模型
│  ├─ schemas.py                请求/响应模型
│  └─ seed_data.py              预置题库数据
├─ tests/
│  ├─ test_app.py               基础测试
│  └─ test_app_extra.py         补充功能测试
├─ .env                         本地 MySQL 配置
├─ .env.example                 配置模板
├─ requirements.txt             依赖列表
├─ README.md                    简版说明
└─ B3_项目说明文档.md            当前总说明文档
```

---

## 5. 数据库说明

### 5.1 当前使用的数据库

当前默认使用 `.env` 中的 `DATABASE_URL` 连接数据库。开发时推荐 MySQL，测试时默认会切换到内存 SQLite。

```env
DATABASE_URL=mysql+pymysql://root:password@127.0.0.1:3306/autograder_b3?charset=utf8mb4
```

数据库名：

```text
autograder_b3
```

### 5.2 已建立的数据表

当前模型对应以下核心表：

- `questions`
  - 题目信息
- `test_cases`
  - 测试用例
- `evaluation_records`
  - 一次评测的总体结果
- `evaluation_case_results`
  - 单个测试用例结果

对应实现：

- [models.py](D:\vscode python\b3\app\models.py)

---

## 6. API 接口说明

### 6.1 健康检查

```http
GET /health
```

返回：

```json
{"status":"ok"}
```

### 6.2 导入题库

```http
POST /api/v1/b3/questions/import/problem-txt
```

作用：

- 将预置的 `Q02-Q10` 和 `API_DEMO` 导入数据库
- 已做幂等处理，重复调用不会重复插入

### 6.3 查询题目列表

```http
GET /api/v1/b3/questions
```

### 6.4 查询题目详情

```http
GET /api/v1/b3/questions/{question_id}
```

### 6.5 更新题目

```http
PUT /api/v1/b3/questions/{question_id}
```

### 6.6 查询题目测试用例

```http
GET /api/v1/b3/questions/{question_id}/cases
```

### 6.7 提交评测

```http
POST /api/v1/b3/evaluate
```

请求示例：

```json
{
  "question_id": "Q02",
  "submitted_code": "ssh user01@127.0.0.1",
  "submission_id": "sub-001",
  "language": "shell"
}
```

### 6.8 标准答案自检

```http
POST /api/v1/b3/evaluate/answer/{question_id}
```

作用：

- 用内置参考答案对题目做一次预评测

---

## 7. 怎么启动和使用

### 7.1 安装依赖

在项目根目录执行：

```powershell
python -m pip install -r requirements.txt
```

### 7.2 检查 `.env`

当前已经为你生成了本地 `.env`：

- [\.env](D:\vscode python\b3\.env)

如果后面要换数据库账号，只需修改其中的 `DATABASE_URL`。

### 7.3 启动服务

```powershell
uvicorn app.main:app --host 0.0.0.0 --port 8003
```

启动后服务地址：

```text
http://127.0.0.1:8003
```

### 7.4 先初始化数据库结构

```powershell
alembic upgrade head
```

### 7.5 先导入题目

服务启动后，先调用：

```http
POST http://127.0.0.1:8003/api/v1/b3/questions/import/problem-txt
```

### 7.6 提交一次评测示例

示例：

```http
POST http://127.0.0.1:8003/api/v1/b3/evaluate
Content-Type: application/json

{
  "question_id": "Q02",
  "submitted_code": "ssh user01@127.0.0.1",
  "submission_id": "demo-001",
  "language": "shell"
}
```

---

## 8. 我已经验证过什么

### 8.1 MySQL 已验证

已经实际完成：

- 使用 `root / 123456` 连接 MySQL
- 创建数据库 `autograder_b3`
- 用 MySQL 配置成功加载应用
- 用 MySQL 配置成功导入题库
- 用 MySQL 配置成功调用评测接口

### 8.2 自动测试已通过

已执行：

```powershell
pytest -q
```

测试结果：

- `14 passed`

当前测试覆盖：

- 健康检查接口
- 导题接口
- 题目列表、详情、测试用例查询
- 题目更新接口
- 不存在题目的 404 分支
- `Q02` 正确答案评测
- `Q02` 命令白名单拦截
- `Q02` 危险 shell 语法拦截
- `Q10` 路径穿越拦截
- `Q10` 死循环特征拦截
- `Q10` 硬编码结果拦截
- `API_DEMO` 正常执行
- `API_DEMO` 危险导入拦截
- 参考答案自检接口

---

## 9. 当前实现方式说明

### 9.1 判题方式

当前 9 道题主要采用：

- 规则匹配
- 命令结构匹配
- 题目特定校验逻辑

也就是说：

- 并不是完整的 Linux 容器级真实执行环境
- 而是针对这 9 道题定制的安全可控判题逻辑

这样做的原因是：

- 你当前明确要求的是只完成 B3
- 这 9 道题本身是固定题
- 用规则化判题更稳定，也更容易验收

### 9.2 文件题与脚本题

部分文件题会在临时目录中构造测试文件，再按规则判断答案是否合法。

`Q10` 会额外检查：

- 是否包含循环结构
- 是否包含判断逻辑
- 是否只是直接硬编码输出结果

---

## 10. 当前限制

当前版本已经可用，但仍有边界需要说明：

- 没有做 JWT 鉴权
- 没有做 Docker 或容器隔离
- 没有接 B1/B2/B4 的真实联调
- 9 道题不是通用 OJ，而是固定题规则判题
- API 调用型框架已实现，但正式业务题还没有用它，只保留了 `API_DEMO`
- 题库导入接口当前仍然是“导入内置种子数据”，还没有真正解析 `problem.txt`
- API 代码执行仍是当前进程内的受限执行，不是容器或独立沙箱执行
- 运行 Python 或 pytest 后，解释器仍会重新生成 `__pycache__` 文件，这属于正常现象

---

## 11. 后续如果你还要继续做，可以怎么扩展

如果后续还要继续完善，建议按这个顺序做：

1. 清理文档、注释和用户可见文案，保证编码与说明一致
2. 将 `import/problem-txt` 改成真正解析 `problem.txt`
3. 将 API 与脚本执行迁移到更安全的隔离执行模型
4. 引入数据库迁移工具，替代启动时自动建表
5. 拆分和重构题目判题规则，降低后续扩展成本
6. 增加更多题目与测试用例管理接口
7. 接入 B2 调用链
8. 接入 B4 鉴权

---

## 12. 关键文件索引

- 服务入口：[main.py](D:\vscode python\b3\app\main.py)
- 配置文件：[config.py](D:\vscode python\b3\app\core\config.py)
- 数据库连接：[db.py](D:\vscode python\b3\app\db.py)
- 数据库模型：[models.py](D:\vscode python\b3\app\models.py)
- 题库数据：[seed_data.py](D:\vscode python\b3\app\seed_data.py)
- 题库服务：[question_service.py](D:\vscode python\b3\app\services\question_service.py)
- 评测服务：[evaluation_service.py](D:\vscode python\b3\app\services\evaluation_service.py)
- Shell 安全检查：[security_checks.py](D:\vscode python\b3\app\judge\security_checks.py)
- 9 道题判题规则：[shell_checks.py](D:\vscode python\b3\app\judge\shell_checks.py)
- API 调用型执行器：[api_runner.py](D:\vscode python\b3\app\judge\api_runner.py)
- 自动测试：[test_app.py](D:\vscode python\b3\tests\test_app.py)
