# AutoGrader B3 API 使用文档

本文档面向 B1、B2、B4 和前端联调方，说明 B3 当前可用接口、请求格式、返回格式、动态新增题目方式，以及远端数据库迁移注意事项。

## 1. 基础信息

本地默认地址：

```text
http://127.0.0.1:8003
```

主要 API 前缀：

```text
/api/v1/b3
```

兼容路由：

```text
/api/v1/questions
/api/v1/evaluate
/api/v1/rules/{question_id}
```

联调建议统一使用 `/api/v1/b3/...`。

## 2. 接口总览

| 方法 | 路径 | 作用 |
| --- | --- | --- |
| GET | `/health` | 健康检查 |
| GET | `/api/v1/b3/questions` | 查询题目列表 |
| POST | `/api/v1/b3/questions` | 新增一道动态题目 |
| GET | `/api/v1/b3/questions/{question_id}` | 查询题目详情 |
| PUT | `/api/v1/b3/questions/{question_id}` | 更新题目基础信息和判题配置 |
| GET | `/api/v1/b3/questions/{question_id}/cases` | 查询题目测试用例 |
| POST | `/api/v1/b3/questions/import/problem-txt` | 导入内置题库 |
| GET | `/api/v1/b3/rules/{question_id}` | 查询静态检查规则 |
| POST | `/api/v1/b3/evaluate` | 正式评测提交 |
| POST | `/api/v1/b3/evaluate/answer/{question_id}` | 运行内置参考答案自测 |

## 3. 当前题型支持

| 前端可传 `question_type` | B3 内部处理 | 适用场景 |
| --- | --- | --- |
| `shell` | `command` | 普通 shell 命令题 |
| `command` | `command` | 普通 shell 命令题 |
| `file` | `file` | 文件读写、复制、重命名、文本处理题 |
| `script` | `script` | 多行 shell 脚本题 |
| `python` | `api` | Python 标准输入输出题或函数调用题 |
| `api` | `api` | Python 函数调用题 |

新增 Python 题默认按标准输入输出模式判题。如果需要函数调用模式，在 `metadata_json` 里配置 `entry_function` 或 `python_mode: "function"`。

## 4. 健康检查

```http
GET /health
```

示例：

```bash
curl http://127.0.0.1:8003/health
```

返回：

```json
{
  "status": "ok"
}
```

## 5. 查询题目列表

```http
GET /api/v1/b3/questions
```

示例：

```bash
curl http://127.0.0.1:8003/api/v1/b3/questions
```

返回字段：

```json
[
  {
    "id": "Q02",
    "title": "SSH 登录题",
    "description": "使用 ssh 命令登录到指定主机并退出",
    "question_type": "command",
    "difficulty": "EASY",
    "language": "shell",
    "time_limit_ms": 2000,
    "memory_limit_mb": 64,
    "allowed_commands": ["ssh", "exit"],
    "metadata_json": {
      "accepted_invocations": ["user01@127.0.0.1", "-l user01 127.0.0.1"],
      "required_exit_command": "exit"
    },
    "status": "ACTIVE"
  }
]
```

## 6. 新增题目

```http
POST /api/v1/b3/questions
Content-Type: application/json
```

字段说明：

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `id` / `question_id` | string | 是 | 题目唯一 ID |
| `title` | string | 是 | 题目标题 |
| `description` | string | 是 | 题面描述 |
| `question_type` | string | 否 | `shell`、`command`、`file`、`script`、`python`、`api` |
| `difficulty` | string | 否 | `EASY`、`MEDIUM`、`HARD` |
| `language` | string | 否 | `shell` 或 `python`，不传时按题型推断 |
| `time_limit_ms` | integer | 否 | 默认 2000 |
| `memory_limit_mb` | integer | 否 | 默认 64 |
| `allowed_commands` | string[] | 否 | shell 题允许使用的命令白名单 |
| `metadata_json` | object | 否 | 判题扩展配置 |
| `status` | string | 否 | 默认 `ACTIVE` |
| `test_cases` / `cases` | array | 否 | 测试用例 |

测试用例字段：

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `case_id` | string | 用例 ID，不传则自动生成 |
| `case_no` | integer | 用例序号，不传则按顺序生成 |
| `description` | string | 用例说明 |
| `input_data` | string/null | 标准输入 |
| `expected_output` | string/null | 期望标准输出 |
| `score_weight` | number | 权重，默认 1.0 |
| `input_files_json` | object/null | 执行前写入工作目录的文件 |
| `expected_files_json` | object/null | 执行后需要校验的文件内容 |
| `call_args_json` | array/null | Python 函数调用题的参数 |
| `is_hidden` | boolean | 是否隐藏用例 |

新增成功返回 `201 Created`。如果题目 ID 已存在，返回 `409 Conflict`。

### 6.1 新增 shell 题示例

请求：

```bash
curl -X POST http://127.0.0.1:8003/api/v1/b3/questions \
  -H "Content-Type: application/json" \
  -d '{
    "id": "Q_FRONTEND_SHELL",
    "title": "Shell 输出题",
    "description": "输出 hello",
    "question_type": "shell",
    "difficulty": "EASY",
    "allowed_commands": ["echo"],
    "test_cases": [
      {
        "case_no": 1,
        "description": "输出 hello",
        "expected_output": "hello",
        "score_weight": 1.0
      }
    ]
  }'
```

正确答案：

```bash
echo hello
```

说明：B3 会兼容结尾换行差异，`hello` 和 `hello\n` 会视为一致。

### 6.2 新增 Python 标准输入输出题示例

请求：

```bash
curl -X POST http://127.0.0.1:8003/api/v1/b3/questions \
  -H "Content-Type: application/json" \
  -d '{
    "id": "Q_FRONTEND_PYTHON",
    "title": "Python 求和题",
    "description": "读入两个整数并输出它们的和",
    "question_type": "python",
    "difficulty": "EASY",
    "metadata_json": {
      "python_mode": "stdin"
    },
    "test_cases": [
      {
        "case_no": 1,
        "description": "两个正整数",
        "input_data": "2 3\n",
        "expected_output": "5",
        "score_weight": 1.0
      }
    ]
  }'
```

正确答案：

```python
a, b = map(int, input().split())
print(a + b)
```

### 6.3 新增 Python 函数调用题示例

请求：

```json
{
  "id": "Q_FUNCTION_ADD",
  "title": "实现 add 函数",
  "description": "实现 add(a, b)，返回两数之和",
  "question_type": "python",
  "difficulty": "EASY",
  "metadata_json": {
    "python_mode": "function",
    "entry_function": "add",
    "python_command": "python3"
  },
  "test_cases": [
    {
      "case_no": 1,
      "description": "正整数",
      "call_args_json": [2, 3],
      "expected_output": "5",
      "score_weight": 1.0
    }
  ]
}
```

正确答案：

```python
def add(a, b):
    return a + b
```

## 7. 查询题目详情

```http
GET /api/v1/b3/questions/{question_id}
```

示例：

```bash
curl http://127.0.0.1:8003/api/v1/b3/questions/Q_FRONTEND_PYTHON
```

返回包含题目基础信息和 `test_cases`。

## 8. 更新题目

```http
PUT /api/v1/b3/questions/{question_id}
Content-Type: application/json
```

可更新字段：

```json
{
  "title": "新的题目标题",
  "description": "新的题目描述",
  "difficulty": "HARD",
  "time_limit_ms": 3000,
  "memory_limit_mb": 128,
  "allowed_commands": ["echo", "cat"],
  "metadata_json": {
    "python_mode": "stdin"
  },
  "status": "ACTIVE"
}
```

说明：`allowed_commands` 和 `metadata_json` 会持久化到数据库，不再只保存在 B3 进程内存中。

## 9. 查询测试用例

```http
GET /api/v1/b3/questions/{question_id}/cases
```

示例：

```bash
curl http://127.0.0.1:8003/api/v1/b3/questions/Q_FRONTEND_PYTHON/cases
```

返回：

```json
[
  {
    "id": 1,
    "case_id": "case_01",
    "case_no": 1,
    "description": "两个正整数",
    "input_data": "2 3\n",
    "expected_output": "5",
    "score_weight": 1.0,
    "input_files_json": null,
    "expected_files_json": null,
    "call_args_json": null,
    "is_hidden": false
  }
]
```

## 10. 查询静态检查规则

```http
GET /api/v1/b3/rules/{question_id}
```

shell 题返回示例：

```json
{
  "question_id": "Q_FRONTEND_SHELL",
  "question_type": "command",
  "language": "shell",
  "allowed_commands": ["echo"],
  "forbidden_modules": ["ctypes", "os", "pathlib", "shutil", "socket", "subprocess", "sys"],
  "forbidden_functions": ["__import__", "compile", "eval", "exec", "open"],
  "metadata_json": {}
}
```

Python stdin 题返回示例：

```json
{
  "question_id": "Q_FRONTEND_PYTHON",
  "question_type": "api",
  "language": "python",
  "allowed_commands": [],
  "forbidden_modules": ["ctypes", "os", "pathlib", "shutil", "socket", "subprocess"],
  "forbidden_functions": ["__import__", "compile", "eval", "exec", "open"],
  "metadata_json": {
    "python_mode": "stdin"
  }
}
```

说明：Python stdin 题默认允许 `sys`，方便使用 `sys.stdin.read()`；其他危险模块仍会被拦截。

## 11. 正式评测

```http
POST /api/v1/b3/evaluate
Content-Type: application/json
```

请求字段：

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `question_id` | string | 是 | 题目 ID |
| `submitted_code` | string | 是 | 学生提交代码，不能为空 |
| `submission_id` | string | 否 | 提交 ID，默认 `manual` |
| `language` | string | 否 | `shell` 或 `python` |

shell 示例：

```bash
curl -X POST http://127.0.0.1:8003/api/v1/b3/evaluate \
  -H "Content-Type: application/json" \
  -d '{
    "question_id": "Q_FRONTEND_SHELL",
    "submitted_code": "echo hello",
    "submission_id": "sub-shell-001",
    "language": "shell"
  }'
```

Python stdin 示例：

```bash
curl -X POST http://127.0.0.1:8003/api/v1/b3/evaluate \
  -H "Content-Type: application/json" \
  -d '{
    "question_id": "Q_FRONTEND_PYTHON",
    "submitted_code": "a, b = map(int, input().split())\nprint(a + b)",
    "submission_id": "sub-python-001",
    "language": "python"
  }'
```

返回：

```json
{
  "question_id": "Q_FRONTEND_PYTHON",
  "submission_id": "sub-python-001",
  "overall_score": 100.0,
  "passed_count": 1,
  "total_count": 1,
  "overall_comment": "通过 1/1 个测试用例。",
  "static_issues": [],
  "case_results": [
    {
      "case_id": "case_01",
      "description": "两个正整数",
      "passed": true,
      "score": 100.0,
      "actual_output": "5\n",
      "expected_output": "5",
      "error": null,
      "execution_time_ms": 20.5
    }
  ]
}
```

## 12. 内置参考答案自测

```http
POST /api/v1/b3/evaluate/answer/{question_id}
```

示例：

```bash
curl -X POST http://127.0.0.1:8003/api/v1/b3/evaluate/answer/Q02
```

说明：该接口只覆盖代码里内置了参考答案的题目，例如 `Q01` 到 `Q10` 和 `API_DEMO`。前端动态新增题目通常使用 `/evaluate` 评测，不使用该接口。

## 13. 错误和状态码

| 场景 | HTTP 状态 | 说明 |
| --- | --- | --- |
| 题目不存在 | 404 | `Question not found` |
| 重复新增题目 | 409 | `Question {id} already exists` |
| 题目没有测试用例 | 422 | `Question {id} has no test cases configured` |
| 提交内容为空 | 422 | 请求体校验失败 |
| 静态检查失败 | 200 | `overall_score` 为 0，查看 `static_issues` |
| 执行失败或超时 | 200 | `overall_score` 为 0，查看 `case_results[].error` |

## 14. 数据库迁移说明

本版本为了支持前端新增题目稳定判题，新增了数据库字段：

```text
questions.judge_config
test_cases.case_id
test_cases.case_no
test_cases.description
test_cases.input_files_json
test_cases.expected_files_json
test_cases.call_args_json
```

对应迁移文件：

```text
alembic/versions/0004_persist_dynamic_judge_fields.py
```

部署到远端数据库时，需要在确认 `DATABASE_URL` 指向正确数据库后执行：

```bash
alembic upgrade head
```

建议执行前先备份远端数据库。

## 15. 最小联调流程

1. `GET /health` 确认 B3 服务在线。
2. `POST /api/v1/b3/questions` 新增题目，或 `POST /api/v1/b3/questions/import/problem-txt` 导入内置题库。
3. `GET /api/v1/b3/questions/{question_id}` 确认题目和测试点存在。
4. `POST /api/v1/b3/evaluate` 提交答案评测。
5. 前端展示 `overall_score`、`overall_comment`、`static_issues`、`case_results`。
